# Project Cado
Testing Angular 2 router for app framework.

To download use below commands
```bash
git clone  https://github.com/lashaalt/project_cado
cd project_cado
npm i
npm start
```
Goto [localhost:3000](http://localhost:3000) for app test
Goto [localhost:3001](http://localhost:3001) for app control and debugging
